# Virtual environment
.venv/

# Chroma vector store (machine-local, lazy-built)
.chroma/

# Runtime artifacts
assets/
reactfirst_run.log
qualified_leads.json

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
.pytest_cache/

# Claude Code machine-local settings (personal allowlist / absolute paths)
.claude/settings.local.json

# Synthetic dev fixtures (not shipped)
brands_catalog.csv
contacts.json
gtm_policies.txt

# Local secrets (real values go here; .env.example is tracked and safe to commit)
.env

# macOS
.DS_Store
