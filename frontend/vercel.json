{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "Proxies the SPA's same-origin /api/* to the Railway backend (backend-production-77e4.up.railway.app) so the browser needs no CORS. api.ts defaults BASE_URL to '/api', so no VITE_API_URL is required. (Touch this file to trigger a Vercel git-integration redeploy.)",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://backend-production-77e4.up.railway.app/api/:path*"
    }
  ]
}
