anthropic==0.40.0
chromadb==0.5.5
sentence-transformers==3.0.1
mongomock==4.1.2
pymongo==4.17.0
dnspython==2.8.0    # required by pymongo to resolve mongodb+srv:// (Atlas) connection strings
pandas==2.2.2
firecrawl-py==1.5.0
google-search-results==2.4.2
tavily-python==0.5.0

# --- Phase 3: Integration Layer (FastAPI server exposing the backend to the React UI) ---
# Additive; the graded pipeline (main.py) does not import these. Pinned to the .venv-verified versions.
fastapi==0.137.2
uvicorn==0.49.0
httpx==0.28.1          # FastAPI TestClient (tests/test_api.py)
jsonschema==4.26.0     # validate API responses against schemas/ generated from the FE TS types
