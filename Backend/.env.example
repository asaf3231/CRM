# Environment variable template for the ReactFirst Outbound Engine.
# Copy this file to .env (gitignored) and fill in real values.
# If MONGO_URI is unset, the application falls back to in-memory mongomock —
# suitable for offline development and the full test suite.

MONGO_URI=mongodb://localhost:27017
DB_NAME=gtm_db
